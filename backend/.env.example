NODE_ENV=development
PORT=8000
API_PREFIX=/api/v1
CORS_ORIGIN=http://localhost:3000
# Docker/local default. If using an installed local PostgreSQL server, copy this
# file to backend/.env and replace the password with your local postgres password.
DATABASE_URL="postgresql://postgres:Anchal%40123@localhost:5432/lexai?schema=public"
JWT_SECRET="dev-super-secret-change-me"
JWT_EXPIRES_IN="7d"
